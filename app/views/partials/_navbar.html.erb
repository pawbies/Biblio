<nav class="bg-gray-200 shadow shadow-gray-300 w-100 px-8 md:px-auto">
	<div class="md:h-16 h-28 mx-auto md:px-4 container flex items-center justify-between flex-wrap md:flex-nowrap">
		<div class="text-indigo-500 md:order-1">
      <h1 class="text-2xl font-extrabold">Biblio</h1>
		</div>
		<div class="text-gray-500 order-3 w-full md:w-auto md:order-2">
			<ul class="flex font-semibold justify-between">
        <li class="md:px-4 md:py-2 text-indigo-500"><a href="<%= root_path %>">Start</a></li>
          <li class="md:px-4 md:py-2 hover:text-indigo-400"><a href="#">Suche</a></li>
        <% if Current.librarian.present? %>
          <li class="md:px-4 md:py-2 hover:text-indigo-400"><a href="<%= borrows_path %>">Verwaltung</a></li>
        <% end %>
			</ul>
		</div>
		<div class="order-2 md:order-3">
      <% if Current.librarian.present? %>
        <%= button_to "Abmelden", logout_path, class: "px-4 py-2 bg-indigo-500 hover:bg-indigo-600 text-gray-50 rounded-xl flex items-center gap-2" %>
      <% else %>
        <%= link_to "Anmelden", login_path, class: "px-4 py-2 bg-indigo-500 hover:bg-indigo-600 text-gray-50 rounded-xl flex items-center gap-2" %>
      <% end %>
		</div>
	</div>
</nav>