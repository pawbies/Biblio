<nav data-controller="nav" class="bg-white shadow fixed top-0 w-full z-50">
  <div class="container mx-auto px-5 py-4 flex justify-between items-center">
    <a href="/" class="text-xl font-bold text-gray-800 flex items-center space-x-2">
      <i class="fas fa-book text-blue-500"></i>
      <span>Biblio</span>
    </a>

    <div class="hidden md:flex space-x-6 items-center">
      <a href="<%= root_path %>" class="block px-2 py-3 text-gray-600 hover:bg-blue-50 hover:text-blue-500 transition">
        <i class="fas fa-home"></i> Home
      </a>
      <a href="<%= search_path %>" class="block px-2 py-3 text-gray-600 hover:bg-blue-50 hover:text-blue-500 transition">
        <i class="fas fa-search"></i> Search
      </a>
      <% if Current.librarian %>
      <a href="<%= borrows_path %>" class="block px-2 py-3 text-gray-600 hover:bg-blue-50 hover:text-blue-500 transition">
        <i class="fas fa-cog"></i> Verwaltung
      </a>
      <a href="<%= logout_path %>" class="block px-2 py-3 text-gray-600 hover:bg-blue-50 hover:text-blue-500 transition">
        <i class="fas fa-user-times"></i> Logout
      </a>
      <% else %>
      <a href="<%= login_path %>" class="block px-2 py-3 text-gray-600 hover:bg-blue-50 hover:text-blue-500 transition">
        <i class="fas fa-user"></i> Login
      </a>
      <% end %>
    </div>

    <button data-action="click->nav#toggle" class="md:hidden text-gray-600 hover:text-blue-500 focus:outline-none">
      <i class="fas fa-bars"></i>
    </button>
  </div>

  <div data-nav-target="mobile" class="hidden bg-gray-100">
    <a href="<%= root_path %>" class="block px-5 py-3 text-gray-600 hover:bg-blue-50 hover:text-blue-500 transition">
      <i class="fas fa-home"></i> Home
    </a>
    <a href="<%= search_path %>" class="block px-5 py-3 text-gray-600 hover:bg-blue-50 hover:text-blue-500 transition">
      <i class="fas fa-search"></i> Search
    </a>
    <% if Current.librarian %>
    <a href="<%= borrows_path %>" class="block px-5 py-3 text-gray-600 hover:bg-blue-50 hover:text-blue-500 transition">
      <i class="fas fa-cog"></i> Verwaltung
    </a>
    <a href="<%= logout_path %>" class="block px-5 py-3 text-gray-600 hover:bg-blue-50 hover:text-blue-500 transition">
      <i class="fas fa-user-times"></i> Logout (Angemeldet als <%= "#{Current.librarian.firstname} #{Current.librarian.lastname}" %>)
    </a>
    <% else %>
    <a href="<%= login_path %>" class="block px-5 py-3 text-gray-600 hover:bg-blue-50 hover:text-blue-500 transition">
      <i class="fas fa-user"></i> Login
    </a>
    <% end %>
  </div>
</nav>