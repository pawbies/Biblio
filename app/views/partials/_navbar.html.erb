<nav class="flex items-center justify-between h-10 bg-gray-200 px-4">
    <%= link_to "Biblio", root_path, class: "font-bold text-gray-700" %>

  <div class="flex">
    <% if Current.librarian.present? %>
        <span class="mr-3">Logged in as <%= "#{Current.librarian.firstname} #{Current.librarian.lastname}" %></span>
        <%= button_to "Logout", logout_path %>
    <% else %>
        <%= link_to "Login", login_path %>
    <% end %>
  </div>
</nav>
